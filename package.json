
{
  "scripts": {
    "start": "node backend/index.js",
     "build": "cd proyecto-econt && npm install && ng build --configuration production"
  },

  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
