
{
  "name": "proyecto-econt-fullstack",
  "version": "1.0.0",
  "description": "Proyecto completo con backend y frontend en Railway",
  "scripts": {
    "install": "cd backend && npm install && cd ../proyecto-econt && npm install",
    "build": "cd proyecto-econt && npx ng build --configuration production",
    "heroku-postbuild": "cd proyecto-econt && npx ng build --configuration production",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }

}
