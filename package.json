
{
  "name": "proyecto-econt-fullstack",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install && cd ../proyecto-econt && npm install",
    "build": "cd proyecto-econt && npx ng build --configuration production",
    "start": "node backend/index.js"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
