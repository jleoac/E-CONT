
{
  "scripts": {
    "build": "cd proyecto-econt && npm install && npx ng build --configuration production",
    "start": "cd backend && npm install && node index.js"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
